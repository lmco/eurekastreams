#siteLabel_txt()
$actor.displayName posted a #activityType($activity) to #streamDescription($stream,$recipient) stream.

#activityContent_txt($activity)

To see the post follow this link:
#activityUrl($stream,$activity)
#parse('Footer.txt')
