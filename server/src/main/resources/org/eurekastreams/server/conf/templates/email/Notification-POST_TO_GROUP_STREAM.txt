#siteLabelText()
$actor.displayName posted a #activityType($activity) to the $stream.displayName stream.

#activityContent_txt($activity)

To see the post follow this link:
#activityUrl($stream,$activity)
#parse('Footer.txt')
