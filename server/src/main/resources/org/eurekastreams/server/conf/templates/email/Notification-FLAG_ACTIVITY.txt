$actor.displayName flagged a #activityType($activity) in #streamDescription($stream,$recipient) stream.

To see the flagged activity follow this link to the Flagged Activities view:
#appUrl($uiUrlBuilder.relativeUrlForFlaggedActivity())
#parse('Footer.txt')
