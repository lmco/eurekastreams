#siteLabelText()
$actor.displayName posted a #activityType($activity) to your stream.

#activityContent_txt($activity)

To see the post follow this link:
#activityUrl($stream,$activity)
#parse('Footer.txt')
